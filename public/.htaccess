SetEnv APPLICATION_ENV development

<IfModule mod_rewrite.c>
    RewriteEngine On
    #RewriteBase /slim-mvc
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
